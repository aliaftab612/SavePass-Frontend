export const DEFAULT_HASH_ITERATIONS: number = 600000;

export enum TwoFactorProviders {
  authenticatorApp = 'AuthenticatorApp',
}

export enum errTypes {
  twoFactorRequired = 'twoFactorRequired',
}
